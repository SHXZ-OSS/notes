# 笔记

「笔记」安卓 App的 Web 端：通过行知·慧云（xzzx-sis）OAuth 登录，浏览与预览同步到 appdata 的笔记数据。Svelte 5 + kampsy/ui + Tailwind CSS v4。

## 开发

```bash
pnpm install
pnpm run dev        # http://localhost:3000
pnpm run build      # 构建到 dist/
pnpm run format     # prettier
```
